import PackageDescription

let package = Package(
    name: "OTPFieldView",
    products: [
        .library(
            name: "OTPFieldView",
            targets: ["OTPFieldView"]),
    ],
    targets: [
        .target(
            name: "OTPFieldView",
            dependencies: []),
    ]
)
